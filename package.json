{
  "name": "cluster-webui-package",
  "packageManager": "yarn@3.2.4",
  "main": "./dist/index.js",
  "workspaces": [
    "client",
    "ipfs-cluster-api"
  ],
  "scripts": {
    "start": "yarn workspace cluster-webui run start",
    "build": "yarn workspaces foreach run build"
  }
}
